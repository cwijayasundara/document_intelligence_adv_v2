# E1-S1: Database Types & ORM Models

**Epic:** E1 — Foundation Infrastructure
**Layer:** Types
**Group:** A
**Depends on:** —

## Description

Define all SQLAlchemy 2.0 ORM models for the platform's 8 core tables plus memory tables. These models are the foundation for all database operations. Uses async-compatible declarative base with UUID primary keys and proper enum types.

## Acceptance Criteria

1. SQLAlchemy ORM models defined for all 8 core tables: documents, document_categories, extraction_schemas, extraction_fields, extracted_values, document_summaries, bulk_jobs, bulk_job_documents
2. Memory models defined: conversation_summaries, memory_entries
3. DocumentStatus enum includes all 7 states: uploaded, parsed, edited, classified, extracted, summarized, ingested
4. All models use UUID primary keys with server-side defaults
5. All models include created_at and updated_at timestamp columns
6. Foreign key relationships defined: documents↔categories, extraction_schemas↔categories, extraction_fields↔schemas, extracted_values↔documents+fields, document_summaries↔documents, bulk_job_documents↔bulk_jobs+documents
