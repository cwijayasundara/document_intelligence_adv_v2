# E2-S2: Document State Machine

**Epic:** E2 — Document Upload & Parsing
**Layer:** Service
**Group:** C
**Depends on:** E2-S1

## Description

Enforces valid document status transitions per the state machine diagram. Prevents illegal transitions (e.g., jumping from uploaded to classified) and provides introspection for available next actions.

## Acceptance Criteria

1. State machine enforces valid transitions: uploaded→parsed, parsed→edited, parsed→classified, edited→classified, classified→extracted, extracted→summarized, summarized→ingested
2. Invalid state transition raises HTTP 400 with message indicating the current state and required prior state
3. Each successful transition updates document.status and document.updated_at atomically
4. get_available_actions(document_id) returns list of valid next transitions for the document's current status
