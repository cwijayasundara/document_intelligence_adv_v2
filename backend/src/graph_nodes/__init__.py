"""LangGraph node functions for document intelligence.

Plain async LLM-task functions (classify, extract, judge, summarize, retrieve)
invoked as nodes from the LangGraph pipeline. Each uses the OpenAI SDK directly
with structured output.
"""
